# Object model

Source of truth types: `shared/src/types.ts`.

## Core hierarchy

- User
  - Folders
    - Groups
      - Bookmarks

## Preferences

User preferences are persisted per user:
- `theme`
- `viewMode` (`tabbed` or `hierarchical`)
